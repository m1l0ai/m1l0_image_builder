ARG BUILDER={{ builder }}

FROM ${BUILDER}

{% for k, v in framework_labels.items() -%}
LABEL "{{ k|lower }}"="{{ v }}"
{% endfor %}

{%- if tags %}
{% for k, v in tags.items() -%}
LABEL "{{ k|lower }}"="{{ v }}"
{% endfor %}
{% endif %}

{{ files }}
{%- if requirements %}
{{ requirements }}
{% endif %}

{{ entrypoint }}